# SPDX-FileCopyrightText: 2026 MainstreamOS
# SPDX-License-Identifier: GPL-3.0-or-later
---
$schema: https://json-schema.org/schema#
$id: https://calamares.io/schemas/installmethod
additionalProperties: false
type: object
properties:
    defaultChoice:
        type: string
        enum: [ default, custom, console, os-only ]
        default: default
    defaultAppsGroupName:
        type: string
